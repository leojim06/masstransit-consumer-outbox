# Masstransit outbox example

* Use the ```docker-compose``` file to run database and broker
* Generate tables with ```Update-Database``` command
* Run API and worker
* Use the API endpoint to create an Order 